/// <reference types="vite/client" />

// Injected by vite.config.ts's `define` — the commit SHA (or timestamp
// fallback) this bundle was built from. See useBuildFreshnessCheck.ts.
declare const __BUILD_ID__: string;
