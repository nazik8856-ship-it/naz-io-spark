// The real deployed function is "process-mission" -- expects { directive: string }
// in the body and returns { solution, explanation, actions } (or { error }).
export const PROCESS_MISSION_FUNCTION = "process-mission";
